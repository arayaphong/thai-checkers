#include "Options.h"

// Note: Most methods are now implemented inline in the header for better optimization.
// Only complex methods that benefit from separate compilation are implemented here.

// All constructors and simple methods are now inline in the header file.
// The complex template methods are also inline for better template instantiation.
